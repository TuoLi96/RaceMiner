CREATE TABLE IF NOT EXISTS lock_use (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	target_file TEXT NOT NULL,
	lock_var TEXT NOT NULL,
	access_var TEXT NOT NULL,
	access_count INTEGER NOT NULL,
	UNIQUE (
		target_file,
		lock_var,
		access_var
	)
);
