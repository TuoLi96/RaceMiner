from setuptools import setup, find_packages

setup(
    name="RaceMinerLib",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.8",
    description="Manager library for DBMgr, PathMgr etc.",
    author="RaceMiner",
)
